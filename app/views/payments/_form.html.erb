<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :num_pago %>
    <%= form.number_field :num_pago, id: :payment_num_pago %>
  </div>

  <div class="field">
    <%= form.label :monto %>
    <%= form.number_field :monto, id: :payment_monto %>
  </div>

  <div class="field">
    <%= form.label :fecha_pago %>
    <%= form.date_select :fecha_pago, id: :payment_fecha_pago %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :payment_status %>
  </div>

  <div class="field">
    <%= form.label :registro %>
    <%= form.text_area :registro, id: :payment_registro %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :payment_client_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
